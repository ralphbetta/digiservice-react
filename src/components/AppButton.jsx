export const AppButton = (title) => {
    return (
        <div className="inline-flex">
                    <button className='bg-primary hover:bg-secondary transition-all duration-300 px-4 py-2 rounded-md text-white' type="button">{title}</button>
                </div>
    )
}
